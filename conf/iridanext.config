iridanext {
    enabled = true
    output {
        path = "${params.outdir}/iridanext.output.json.gz"
        overwrite = true
        validate = true
        files {
            idkey = "sample"
            samples = ["**/reads/*.fastq.gz"]
        }
    }
}
